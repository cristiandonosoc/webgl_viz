import Renderer from "./renderer";

// export function CanvasToLocal(dimensions: number[], offset: number[], scale: number[],
//                               point: number[]) {

//   // Local (variable space)
//   // Convert from pixels to 0.0 -> 1.0
//   var temp = [point[0] / dimentions[0],
//               point[1] / dimensions[0]];
//   temp = temp.map(i => (i * 2.0) - 1.0);

//   // De-apply offset and scale
//   var local = [(temp[0] - offset[0]) / scale[0],
//                (temp[1] + offset[1]) / scale[1]];
// };

// export function CanvasToLocal(renderer: Renderer, point: number[]) {
//   var dimensions = [
//   var dimensions = [this.renderer.gl.canvas.width,
//                     this.renderer.gl.canvas.height];
//   var offset = this.renderer.state
//   return CanvasToLocal(

// }
